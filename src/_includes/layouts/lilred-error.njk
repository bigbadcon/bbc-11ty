{% extends 'layouts/base.njk' %}

{% block content %}
    <main id="content">
        <div class="mx-auto max-w-content p-base">
            <h1 class="font-serif text-4xl font-bold leading-none md:leading-sub md:text-5xl text-header">{{ title | safe }}</h1>
            <div class="">{{ content | safe }}</div>
            <div class="border-2 rounded-md p-base bg-card mt-lg border-highlight">Sorry our event manager service is having trouble at the moment. We are looking into it. Please check back in a few hours.</div>
        </div>
    </main>
{% endblock %}